spring:
  application:
    name: servicio-items
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: refresh